import React from 'react';
import styled from 'styled-components';

const Index = ({ className }: StyledProps) => (
  <div className={className}>Web</div>
);

export default styled(Index)`
  font-size: 50px;
`;
